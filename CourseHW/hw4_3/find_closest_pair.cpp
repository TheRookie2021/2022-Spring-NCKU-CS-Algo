#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
typedef pair<int,int> POS;

POS find_closest(vector<POS> v, POS target){
    int l=0, r=v.size()-1;
    
}

int main(){
    vector<POS> v;
    POS target;
    // input

    sort(v.begin(),v.end());

    return 0;
}